# 昆虫标本图片识别与 Excel 录入工作台——开发清单

> 本文可以直接交给 GLM 智谱或其他代码开发模型执行。

## 1. 项目目标

开发一个简洁、美观、中文界面的本地网站工作台，用于处理一次性的昆虫标本信息录入项目。

用户首先在“设置”页面上传一份 Excel 模板，并配置第三方视觉大模型 API。之后用户每次上传一张昆虫标本图片，系统先调用视觉大模型，只识别图片中出现的 5 项原始信息。用户核查这些信息，可以选择重新识别，也可以直接修改错误字段。用户点击确认后，系统再以确认后的中名为依据自动补全 8 项分类信息、保存记录并更新 Excel 预览。最后把所有完成记录写入 Excel 模板并下载。

识别工作台还需要实时显示目标 Excel 工作表。每次图片信息识别完成后，待确认数据立即以临时行显示在 Excel 预览区；用户修改字段时，预览行同步更新；确认后系统自动完成分类，该行转为正式记录并显示下一条记录将写入的位置。

### 核心原则

- 单用户、本地运行。
- 一次只上传并识别一张图片。
- 不需要登录、账号、权限、支付、对象存储或复杂安全体系。
- 原始 Excel 模板永远不覆盖。
- Excel 只作为模板和导出格式，SQLite 数据库保存识别记录。
- AI 结果必须经过可编辑表单确认后才能进入最终导出数据。
- 人工只负责核查图片中直接提取的 5 项信息；分类补全、整理、保存和填表由系统自动完成。
- 必须先确认图片提取信息，再执行分类补全，禁止直接用未经确认的中名进行分类。
- 只填写指定的 13 个 Excel 字段，其他单元格不修改。
- 工作台必须提供只读的 Excel 实时预览，预览内容与最终导出数据保持一致。
- Excel 预览区不直接编辑，所有修改统一通过右侧识别结果表单完成。

---

## 2. 技术栈

### 前端

- React
- Vite
- TypeScript
- Tailwind CSS
- shadcn/ui 或同类简洁组件库
- Axios 或原生 Fetch
- Lucide Icons

### 后端

- Python 3.11+
- FastAPI
- SQLAlchemy
- SQLite
- Pydantic
- openpyxl
- httpx
- Pillow
- python-multipart

### 运行方式

- 开发时前后端分别运行。
- 最终构建后，由 FastAPI 托管前端静态文件，整个项目只需启动一个 Python 服务。
- 提供 Windows 可使用的启动脚本。

---

## 3. 不在本期范围内

- 用户注册和登录
- 多用户协作
- 权限控制
- 云端对象存储
- 在线支付
- 批量上传图片
- 手机端专项适配
- Excel 单元格直接编辑（本期只实现只读的 Excel 实时预览）
- 自动联网抓取分类学数据库
- 复杂数据统计报表
- 模型微调

---

## 4. 需要填写的 Excel 字段

系统只填写以下 13 个字段：

1. 中名
2. Phylum
3. 纲
4. Class
5. Order
6. 中文科名
7. 科名
8. 属名
9. 种名
10. 产地3
11. 图像
12. 采集人
13. 采集日期

### 字段来源

#### 从图片直接识别

- 中名
- 产地3
- 图像
- 采集人
- 采集日期

#### 根据中名补全分类

- Phylum
- 纲
- Class
- Order
- 中文科名
- 科名
- 属名
- 种名

### 字段格式要求

| 字段 | 格式要求 |
| --- | --- |
| 中名 | 保留图片中的中文鉴定名 |
| Phylum | 拉丁文门名，例如 `Arthropoda` |
| 纲 | 中文纲名，例如 `昆虫纲` |
| Class | 拉丁文纲名，例如 `Insecta` |
| Order | 拉丁文目名，例如 `Hemiptera` |
| 中文科名 | 中文科名，例如 `红蝽科` |
| 科名 | 拉丁文科名，例如 `Pyrrhocoridae` |
| 属名 | 拉丁文属名，例如 `Dysdercus` |
| 种名 | 只填写种加词，例如 `cingulatus`，不要填写完整双名 |
| 产地3 | 去除“深圳”或“深圳市”等上级地名后，保留具体区、景区或采集点 |
| 图像 | 图片标签中的标本/图像编号，例如 `PSZP-00842`，不是上传的 JPG 文件名 |
| 采集人 | 图片中不存在或无法确认时留空，不得猜测 |
| 采集日期 | 标准格式 `YYYY-MM-DD` |

---

## 5. 页面结构

左侧使用固定侧边栏，包含：

- 识别工作台
- 记录管理
- Excel 导出
- 设置

侧边栏底部显示：

- 当前模板名称
- 当前模型名称
- API 连通状态

## 5.1 识别工作台

### 页面布局

采用左右两栏布局。

#### 左侧：图片区

- 拖拽上传区域
- 点击选择图片
- 支持 JPG、JPEG、PNG、WebP
- 图片预览
- 放大、缩小
- 顺时针旋转
- 逆时针旋转
- 重新选择图片
- 显示上传文件名

图片处理规则：

- 必须保留用户上传的原图，不直接改写原图。
- 读取图片时先应用 EXIF 方向纠正。
- 用户点击旋转后，前端记录实际旋转角度 `0/90/180/270`；开始识别或重新识别时必须将该角度提交给后端。
- 后端把用户旋转应用到真正发送给模型的识别图片，不能只用 CSS 旋转前端预览。
- 模型输入图统一转换为 RGB；长边超过 3000 像素时等比例缩小；使用 JPEG 质量 90 生成临时识别图。
- 临时识别图保存在独立目录，记录完成后可清理；记录管理中查看的仍是原图。

#### 右侧：图片信息确认

分为两个卡片：

1. 图片原始信息确认
2. 分类信息

图片原始信息字段：

- 中名
- 产地3
- 图像
- 采集人
- 采集日期

分类信息字段：

- Phylum
- 纲
- Class
- Order
- 中文科名
- 科名
- 属名
- 种名

交互规则：

- 第一次识别完成后，只填写并开放编辑 5 个图片原始信息字段。
- 分类信息卡片暂时锁定，并显示“请先确认图片信息”。
- 用户可以直接修改任意图片原始信息字段。
- 用户点击确认后，系统才开始生成分类信息。
- 分类完成后，8 个分类字段自动显示，并随记录一起保存和写入 Excel 预览。
- 分类结果默认不要求用户再次确认；如果后续发现问题，可以在“记录管理”中编辑。
- “中名”和“图像”是确认入表前的必填项；任一为空时禁用“确认信息并自动入表”，并在对应字段下显示中文错误提示。
- “采集人”允许为空；“产地3”或“采集日期”为空时显示黄色警告，但允许用户确认继续。

### 状态显示

- 未识别：灰色
- 正在提取图片信息：蓝色加载状态
- 等待确认图片信息：黄色
- 正在整理分类信息：紫色加载状态
- 已完成并填表：绿色
- 存在不确定字段：黄色
- 图片识别失败：红色
- 分类失败：红色

后端状态值固定为：

```text
uploaded
extracting
awaiting_confirmation
classifying
completed
extraction_failed
classification_failed
```

主流程：

```text
uploaded
→ extracting
→ awaiting_confirmation
→ classifying
→ completed
```

点击“重新识别”时，从 `awaiting_confirmation` 返回 `extracting`。

### 操作按钮

- 开始识别
- 重新识别
- 确认信息并自动入表
- 清空当前图片

“重新识别”的行为：

- 使用当前原图再次调用视觉模型。
- 只重新提取 5 个图片原始信息字段。
- 用新的识别结果替换当前草稿。
- 不生成分类信息，不保存正式记录。
- 用户仍可继续修改并再次重新识别。

“确认信息并自动入表”的行为：

- 以用户当前看到并修改后的 5 项信息作为最终确认信息。
- 提交前再次校验“中名”和“图像”不为空。
- 后端以“图像”编号检查重复记录；默认不得静默新增重复编号。
- 如果编号已存在，接口返回 `409 Conflict` 和已有记录编号，前端弹窗让用户选择“覆盖已有记录”或“取消”。
- 用户选择覆盖时更新已有记录并保持其原有排序位置，同时删除本次新建的重复草稿记录；用户取消时保留当前草稿。
- 锁定本次确认值，后续分类不得使用最初的模型原始值。
- 使用确认后的中名查询分类缓存并补全 8 个分类字段。
- 缓存中没有时调用大模型分类。
- 8 个分类字段通过自动校验后，完成 13 个字段并写入 SQLite。
- 当前记录状态设为“已完成”。
- 图片保留在本地数据目录。
- Excel 预览区中的临时行变为正式行。
- Excel 预览区自动定位到最新确认行和下一条写入位置。
- 页面显示“已写入 Excel 第 N 行”，最新行保持绿色高亮约 2 秒。
- 2 秒后自动清空当前工作区并聚焦上传区域，等待下一张图片。

### Excel 实时预览区

识别工作台下方增加一个全宽的“Excel 实时预览”区域。预览区用于实时展示目标工作表内容和填表进度，不允许直接编辑单元格。

#### 基础显示

- 显示当前目标工作表名称。
- 显示 Excel 列字母，例如 A、B、C。
- 显示行号。
- 显示表头和已有模板数据。
- 默认显示目标表头行到最新写入行附近。
- 支持水平和垂直滚动。
- 表头行和行号固定。
- 支持 75%、100%、125% 缩放。
- 提供“全部列”和“仅显示 13 个目标字段”切换。
- 默认使用“仅显示 13 个目标字段”。

#### 实时更新规则

1. 上传 Excel 模板并完成字段映射后，立即加载目标工作表预览。
2. 图片信息识别完成但尚未确认时，在下一条写入位置增加一行临时预览数据。
3. 临时预览行只显示 5 个图片原始信息字段，8 个分类字段暂时留空。
4. 临时预览行使用淡黄色背景，并显示“待确认图片信息”标签。
5. 用户修改右侧 5 项信息时，临时预览行对应单元格实时同步。
6. 用户点击“确认信息并自动入表”后，系统进入分类处理中，临时行显示加载状态。
7. 分类完成后，8 个分类字段自动出现在同一行，临时行变为正式行。
8. 新完成行使用淡绿色闪烁或高亮约 2 秒，然后恢复普通样式。
9. 预览区自动滚动到最新完成行。
10. 下一条空白写入行使用蓝色边框提示。
11. 用户删除或编辑历史记录后，预览区立即重新计算并刷新。
12. 最终导出的记录顺序必须与预览区正式记录的顺序一致。

#### 预览精度边界

- 本区域是只读的 Excel 风格数据网格，用于核对字段值、目标列、行号和处理进度。
- 预览必须准确显示单元格值、列字母、行号、目标表头和写入位置。
- 不要求像 Microsoft Excel 一样像素级还原合并单元格、打印布局、图片对象和全部复杂样式。
- 最终文件的原模板结构与样式保留情况以实际导出的 `.xlsx` 文件为准。

#### 单元格显示规则

- 13 个目标字段列使用轻微蓝绿色底色标识。
- 非目标字段保持只读并使用普通背景。
- 当前临时行的 5 个图片信息单元格使用黄色背景，8 个分类单元格显示灰色等待状态。
- 已完成的最新一行使用绿色提示。
- 空字符串显示为空白，不显示 `null`、`undefined` 或 `None`。
- 日期统一显示为 `YYYY-MM-DD`。
- 长文本单元格使用省略号，鼠标悬停显示完整内容。

#### 状态栏

预览区底部显示：

- 当前工作表
- 当前已完成记录数
- 当前预览总行数
- 最新写入行号
- 下一条写入行号
- 最近更新时间

#### 性能要求

- 默认只加载表头、模板已有数据以及最近 100 条记录。
- 提供“加载更多”按钮。
- 行数较多时使用虚拟滚动。
- 不需要接入完整的在线 Excel 编辑器，使用只读数据网格即可。

## 5.2 记录管理

使用表格显示：

- 图片缩略图
- 图像编号
- 中名
- 产地3
- 采集人
- 采集日期
- 分类状态
- 记录状态
- 创建时间
- 操作

### 功能

- 按图像编号搜索
- 按中名搜索
- 按状态筛选
- 编辑记录
- 删除记录
- 查看原图
- 重新调用 AI 识别
- 对历史记录执行编辑并重新完成分类

### 状态

- 待识别
- 图片信息识别中
- 待确认图片信息
- 分类整理中
- 已完成
- 识别失败

## 5.3 Excel 导出

显示：

- 已完成记录数
- 待确认图片信息记录数
- 当前模板名称
- 目标工作表
- 开始写入行

提供：

- 只导出已完成记录
- 下载 Excel

导出文件名：

```text
昆虫标本信息_YYYYMMDD_HHmm.xlsx
```

## 5.4 设置

分为三个部分。

### A. Excel 模板设置

- 上传 `.xlsx` 文件
- 显示模板文件名
- 显示工作表列表
- 选择目标工作表
- 选择表头行
- 设置开始写入行
- 设置格式来源行
- 显示 13 个字段的列映射
- 自动匹配同名表头
- 无法自动匹配时允许通过下拉框手动选择
- 保存模板配置
- 测试模板配置

### B. 模型 API 设置

- Base URL
- API Key
- 模型名称
- 测试连接
- 保存配置

设置页面只允许用户填写以上三项，不显示服务商、超时时间、重试次数或其他高级参数。

参数约定：

- `base_url` 必须填写 API 根地址，例如 `https://example.com/v1`，不能填写到 `/chat/completions` 等具体接口路径。
- `model_name` 对应的同一个模型必须同时支持图片输入和纯文本 JSON 分类。
- “测试连接”不能只测试文字聊天：必须分别执行一次最小图片输入测试和一次最小文本 JSON 分类测试。
- 任一测试失败时，显示明确中文原因，例如“当前模型不支持图片输入”“Base URL 不是 API 根地址”或“模型未返回合法 JSON”。

后端配置字段固定为：

```json
{
  "base_url": "",
  "api_key": "",
  "model_name": ""
}
```

系统使用统一的 OpenAI 兼容多模态请求格式。只要目标模型支持 OpenAI 兼容的图片输入接口，用户更换以上三项即可切换模型，不需要修改代码。

请求超时和重试次数作为程序内部常量处理，默认超时 120 秒、失败后最多自动重试 2 次，不提供设置界面。

### C. 提示词设置

- 视觉识别系统提示词
- 分类补全提示词
- 恢复默认提示词
- 保存提示词

---

## 6. 推荐项目目录

```text
insect-workbench/
├─ backend/
│  ├─ app/
│  │  ├─ main.py
│  │  ├─ config.py
│  │  ├─ database.py
│  │  ├─ models.py
│  │  ├─ schemas.py
│  │  ├─ routers/
│  │  │  ├─ settings.py
│  │  │  ├─ templates.py
│  │  │  ├─ recognition.py
│  │  │  ├─ records.py
│  │  │  └─ export.py
│  │  ├─ services/
│  │  │  ├─ model_provider.py
│  │  │  ├─ recognition_service.py
│  │  │  ├─ taxonomy_service.py
│  │  │  ├─ template_service.py
│  │  │  ├─ preview_service.py
│  │  │  └─ excel_service.py
│  │  └─ prompts/
│  │     ├─ recognition_prompt.txt
│  │     └─ taxonomy_prompt.txt
│  ├─ requirements.txt
│  └─ tests/
├─ frontend/
│  ├─ src/
│  │  ├─ pages/
│  │  ├─ components/
│  │  ├─ services/
│  │  ├─ types/
│  │  └─ App.tsx
│  ├─ package.json
│  └─ vite.config.ts
├─ data/
│  ├─ templates/
│  ├─ images/
│  ├─ processed_images/
│  ├─ exports/
│  └─ app.db
├─ scripts/
│  ├─ start.bat
│  └─ start.sh
├─ .env.example
└─ README.md
```

---

## 7. 数据库设计

## 7.1 `app_settings`

保存单例配置：

- id
- base_url
- api_key
- model_name
- recognition_prompt
- taxonomy_prompt
- created_at
- updated_at

## 7.2 `excel_templates`

- id
- original_filename
- stored_path
- target_sheet
- header_row
- start_row
- base_write_row
- style_source_row
- field_mapping_json
- is_active
- created_at

其中 `base_write_row` 在模板配置保存时确定：从 `start_row` 开始，在“图像”列中查找第一个可写空白行；若没有空白行，则使用当前最后一个已用行的下一行。预览和导出必须始终使用这个已保存的行号，不得在每次导出时重新扫描。

## 7.3 `specimen_records`

- id
- image_filename
- image_path
- processed_image_path
- rotation_degrees
- status
- raw_model_response
- extracted_draft_json
- confirmed_extraction_json
- taxonomy_result_json
- warnings_json
- 中名
- Phylum
- 纲
- Class
- Order
- 中文科名
- 科名
- 属名
- 种名
- 产地3
- 图像
- 采集人
- 采集日期
- created_at
- updated_at

约束：

- “图像”编号在已完成记录中必须唯一；确认时由业务层和数据库唯一索引共同防止重复。
- 草稿阶段允许“图像”为空，进入 `completed` 状态前必须校验非空。
- 覆盖重复编号时更新原记录，不创建新的已完成记录。

## 7.4 `taxonomy_cache`

用于缓存已经确认的中名与分类信息：

- id
- 中名
- Phylum
- 纲
- Class
- Order
- 中文科名
- 科名
- 属名
- 种名
- created_at
- updated_at

规则：

- 只有分类成功且 8 个分类字段全部通过自动校验后，才能保存或更新缓存。
- 尚未完成分类的记录、分类字段不完整的记录和 `classification_failed` 记录不得写入缓存。
- 后续识别到完全相同的中名时，优先使用已经标记为有效的缓存，不重复让模型生成分类。
- 从记录管理中修改分类信息时，只有修改后的 8 个字段再次通过校验，才同步更新缓存。
- 不得因为第一条分类结果未经校验而把错误结果传播给后续记录。

---

## 8. 后端接口

## 8.1 设置

```text
GET    /api/settings
PUT    /api/settings
POST   /api/settings/test-model
GET    /api/settings/prompts
PUT    /api/settings/prompts
```

## 8.2 Excel 模板

```text
POST   /api/templates/upload
GET    /api/templates/current
GET    /api/templates/{id}/sheets
POST   /api/templates/{id}/inspect
PUT    /api/templates/{id}/mapping
POST   /api/templates/{id}/test
```

## 8.3 图片识别

```text
POST   /api/recognition/extract
POST   /api/recognition/{record_id}/re-extract
POST   /api/recognition/{record_id}/confirm-extraction
```

本期统一使用同步 HTTP 流程：

- 前端发起请求后保持加载状态，等待该请求直接返回结果。
- 不创建 `job_id`，不实现轮询接口，也不使用 FastAPI 后台任务处理本流程。
- `extract` 和 `re-extract` 等待图片识别完成后返回 5 项草稿。
- `confirm-extraction` 等待分类、校验和保存完成后返回最终 13 项以及实际 Excel 行号。

`POST /api/recognition/extract`：

- 接收一张图片。
- 保存图片。
- 创建记录。
- 接收旋转角度并生成实际用于模型输入的预处理图片。
- 调用视觉模型提取 5 个图片原始信息字段。
- 校验 JSON。
- 状态设为“待确认图片信息”。
- 返回可编辑的 5 个图片原始信息字段。

`POST /api/recognition/{record_id}/re-extract`：

- 使用同一张原图重新调用视觉模型。
- 重新提取 5 个图片原始信息字段。
- 替换当前草稿。
- 不执行分类，不写入 Excel 正式预览。

`POST /api/recognition/{record_id}/confirm-extraction`：

- 请求体必须包含用户最终确认或修改后的 5 个图片信息字段。
- 请求体中的“中名”和“图像”不能为空。
- 默认检查“图像”编号是否已存在；若存在则返回 `409 Conflict`、`existing_record_id` 和已有记录摘要。
- 用户明确选择覆盖后，前端以 `duplicate_action: "replace"` 重新提交；后端更新已有记录并清理本次重复草稿，不新增重复行。
- 将这 5 项保存到 `confirmed_extraction_json`。
- 只使用确认后的中名查询分类缓存或调用模型分类。
- 自动生成 8 个分类字段。
- 自动校验分类字段；首次不通过时允许模型自动纠正重试 1 次。
- 重试后仍不通过时保留已确认的 5 项，状态设为“分类失败”，不得写入缓存或 Excel 正式记录。
- 合并成最终 13 个字段。
- 状态设为“已完成”。
- 保存记录并刷新 Excel 正式预览。
- 返回最终 13 个字段和 `excel_row`。

## 8.4 记录

```text
GET    /api/records
GET    /api/records/{id}
PATCH  /api/records/{id}
DELETE /api/records/{id}
POST   /api/records/{id}/reclassify
```

`POST /api/records/{id}/reclassify` 只允许对已经保存 `confirmed_extraction_json` 的记录重新执行分类、校验和入表，不重新调用图片识别，也不要求用户再次确认 5 项图片信息。

## 8.5 导出

```text
GET    /api/export/summary
POST   /api/export/excel
GET    /api/export/download/{filename}
```

## 8.6 Excel 实时预览

```text
GET    /api/excel/preview
GET    /api/excel/preview?mode=target&limit=100
GET    /api/excel/preview?mode=all&limit=100
```

说明：

- `mode=target`：只返回 13 个目标字段。
- `mode=all`：返回目标工作表的全部列。
- 后端返回模板已有行和数据库中的已完成记录。
- 当前尚未确认的草稿由后端从 SQLite 返回，前端再根据右侧表单的即时修改叠加为黄色临时行。
- 临时行不进入已完成记录集，也不参与 Excel 导出。

返回结构示例：

```json
{
  "sheet_name": "实际要录入的表格",
  "mode": "target",
  "header_row": 1,
  "base_write_row": 8,
  "columns": [
    {"letter": "E", "field": "中名"},
    {"letter": "G", "field": "Phylum"},
    {"letter": "H", "field": "纲"}
  ],
  "rows": [
    {
      "excel_row": 8,
      "record_id": 1,
      "status": "completed",
      "values": {
        "中名": "二点红蝽",
        "Phylum": "Arthropoda"
      }
    }
  ],
  "completed_count": 1,
  "latest_write_row": 8,
  "next_write_row": 9,
  "last_updated": "2026-07-28T15:30:00"
}
```

---

## 9. 大模型调用层

项目不需要开发多个服务商适配器，只实现一个通用的 OpenAI 兼容多模态客户端。

定义统一接口：

```python
class VisionModelClient:
    async def recognize_image(
        self,
        image_path: str,
        prompt: str,
    ) -> dict:
        raise NotImplementedError

    async def complete_taxonomy(
        self,
        common_name: str,
        prompt: str,
    ) -> dict:
        raise NotImplementedError
```

这里的 `async` 只是后端内部等待网络请求的实现方式；API 路由必须 `await` 完成并在同一次 HTTP 响应中返回结果，不代表创建后台任务或前端轮询。

配置参数：

- `base_url`
- `api_key`
- `model_name`

客户端约定：

- `base_url` 按 API 根地址处理，客户端统一在内部拼接兼容接口路径。
- 同一个 `model_name` 同时用于图片提取和文本分类。
- 设置页“测试连接”必须调用一次最小图片请求和一次最小文本 JSON 请求，确认两种能力都可用。

图片传输方式：

- 首选 Base64 Data URL。
- 请求使用 OpenAI 兼容的 `messages` 图文混合格式。
- 不开发各厂商专有请求格式。

发送图片前：

1. 使用 Pillow 的 `ImageOps.exif_transpose` 纠正 EXIF 方向。
2. 应用前端提交的 `0/90/180/270` 旋转角度。
3. 转换为 RGB。
4. 长边超过 3000 像素时等比例缩小。
5. 以 JPEG 质量 90 生成临时模型输入图。
6. 保留原图路径和处理图路径，模型只读取处理图。

模型返回不是合法 JSON 时：

1. 尝试提取 Markdown 代码块中的 JSON。
2. 尝试截取第一个 `{` 到最后一个 `}`。
3. 校验字段。
4. 仍失败时自动重试。
5. 图片提取连续失败 2 次后标记“图片识别失败”。
6. 分类补全按第 11 节的独立校验与纠正规则处理，失败时标记“分类失败”。

---

## 10. 大模型提示词

## 10.1 图片信息提取提示词

默认系统提示词：

```text
你是昆虫标本标签信息提取助手。

请分析用户上传的昆虫标本图片。图片中可能包含手写中名标签、采集地点标签、图像或标本编号标签、采集人和采集日期。

本次只提取以下 5 个图片原始信息字段：
中名、产地3、图像、采集人、采集日期。

规则：
1. 中名、产地3、图像、采集人、采集日期必须来自图片。
2. 图像字段是图片内标签上的编号，例如 PSZP-00842，不是上传文件名。
3. 采集人没有填写、无法看清或无法确认时必须返回空字符串，禁止猜测。
4. 采集日期输出为 YYYY-MM-DD。
5. 日期中的罗马数字 I 至 XII 代表月份。例如 X-24, 2009 转换为 2009-10-24。
6. 产地3保留具体区、景区或采集点，去除“深圳”或“深圳市”等上级地名。例如“深圳龙岗园山景区”转换为“龙岗园山景区”。
7. 本次禁止返回或推断 Phylum、纲、Class、Order、中文科名、科名、属名、种名。
8. 对任何不确定字段，不要编造。保留最可能的候选值，并加入 warnings。
9. 只输出 JSON，不要输出 Markdown、解释或思考过程。
```

要求返回：

```json
{
  "中名": "",
  "产地3": "",
  "图像": "",
  "采集人": "",
  "采集日期": "",
  "confidence": {
    "中名": "high",
    "产地3": "high",
    "图像": "high",
    "采集人": "low",
    "采集日期": "medium"
  },
  "evidence": {
    "中名": "",
    "产地3": "",
    "图像": "",
    "采集人": "",
    "采集日期": ""
  },
  "warnings": []
}
```

## 10.2 分类补全提示词

只有用户确认 5 项图片信息后才允许执行本提示词。传入的中名必须来自 `confirmed_extraction_json`，禁止读取或使用最初的模型草稿。

```text
你是昆虫分类信息整理助手。

用户已经人工确认昆虫中名。请只根据这个确认后的中名补全分类信息。

确认后的中名：
{{confirmed_common_name}}

需要返回：
Phylum、纲、Class、Order、中文科名、科名、属名、种名。

规则：
1. Phylum、Class、Order、科名、属名和种名使用拉丁文。
2. 纲和中文科名使用中文。
3. 种名只填写种加词，不填写完整双名。
4. 例如 Dysdercus cingulatus：属名为 Dysdercus，种名为 cingulatus。
5. 不要修改或重新解释用户确认的中名。
6. 无法确定时返回空字符串，并在 warnings 中说明。
7. 只输出 JSON，不要输出 Markdown、解释或思考过程。
```

要求返回：

```json
{
  "Phylum": "",
  "纲": "",
  "Class": "",
  "Order": "",
  "中文科名": "",
  "科名": "",
  "属名": "",
  "种名": "",
  "warnings": []
}
```

---

## 11. 分类信息处理

处理顺序：

1. 接收用户最终确认或修改后的 5 项图片信息。
2. 把确认值保存到 `confirmed_extraction_json`。
3. 读取确认后的中名，禁止读取模型最初草稿中的中名。
4. 查询 `taxonomy_cache`。
5. 如果存在完全相同且校验状态有效的中名，读取缓存中的 8 个分类字段并再次执行结构校验。
6. 如果没有缓存，调用分类补全提示词生成 8 个分类字段。
7. 对 8 个分类字段执行自动校验。
8. 首次校验失败时，将具体错误交给模型自动纠正并重试 1 次。
9. 校验通过后，将确认后的 5 项信息与 8 项分类信息合并成最终 13 个字段。
10. 自动保存记录并更新 Excel 预览，不要求用户进行第二次确认。
11. 只有校验通过的分类结果才写入 `taxonomy_cache`，供相同中名复用。
12. 如果分类调用或纠正失败，保留已经确认的 5 项信息，状态设为“分类失败”，允许在记录管理中单独重试分类，无须重新识别图片。

自动校验至少包括：

- 8 个分类字段全部存在且非空。
- `Phylum`、`Class`、`Order`、`科名`、`属名`、`种名`只包含合理的拉丁字母、连字符或必要空格。
- `纲`和`中文科名`必须包含中文字符。
- `属名`首字母大写，`种名`首字母小写。
- `种名`只能是种加词，不能包含属名或完整双名。
- 同一中名命中缓存时仍执行上述校验；无效缓存立即停用并重新调用模型。

禁止：

- 在用户确认 5 项图片信息之前执行分类。
- 使用 `extracted_draft_json` 中未经确认的中名进行分类。
- 根据图片中昆虫外形重新猜测物种，除非图片标签完全没有中名且用户主动要求。
- 将完整双名写入“种名”列。
- 将中文名写入拉丁文字段。

---

## 12. Excel 模板处理

## 12.1 上传模板

- 仅处理 `.xlsx`。
- 将上传文件保存到 `data/templates/`。
- 数据库保存原始文件名和实际路径。
- 上传新模板后，旧模板可以保留，但只有一个模板处于启用状态。

## 12.2 表头识别

- 读取所有工作表名称。
- 默认扫描前 20 行寻找表头。
- 如果某行能匹配多个目标字段，将该行作为候选表头。
- 前端允许用户修改表头行。
- 自动生成字段到列的映射。

示例：

```json
{
  "中名": "E",
  "Phylum": "G",
  "纲": "H",
  "Class": "I",
  "Order": "K",
  "中文科名": "L",
  "科名": "M",
  "属名": "N",
  "种名": "O",
  "产地3": "X",
  "图像": "AE",
  "采集人": "AI",
  "采集日期": "AJ"
}
```

## 12.3 导出规则

导出时：

1. 使用原模板创建副本。
2. 打开目标工作表。
3. 读取模板配置中已保存的 `base_write_row`。
4. 读取所有 `completed` 记录并按 `id` 升序形成稳定顺序。
5. 第 `index` 条记录（从 0 开始）的 Excel 行号固定为 `base_write_row + index`。
6. 只写入 13 个目标字段。
7. 保留 `base_write_row` 之前的模板原有行、其他列的值和格式，不得改动。
8. 从格式来源行复制目标单元格的字体、填充、边框、对齐和数字格式。
9. 采集日期写为真正的 Excel 日期，并使用 `yyyy-mm-dd` 格式。
10. 图像编号以文本格式写入。
11. 导出到 `data/exports/`。
12. 返回下载地址。
13. 不覆盖原模板。

补充规则：

- 每次预览和导出都从原模板加数据库中的已完成记录重新构建，不在上一次导出文件上追加。
- 删除记录后，其后的记录在下一次预览和导出中依次前移，不留下由本系统造成的空洞。
- 覆盖重复“图像”编号时更新原记录，保持该记录原有 `id` 和 Excel 排序位置。
- 禁止在每次导出时重新扫描“图像”列寻找空白行，否则预览行号与导出行号可能不一致。

## 12.4 实时预览规则

- Excel 预览与 Excel 导出必须使用同一套字段映射、记录排序和日期格式化逻辑。
- `preview_service.py` 负责把模板已有行与数据库中的已完成记录合并成预览数据。
- `excel_service.py` 负责把同一批已完成记录写入模板副本。
- 两个服务必须共享同一个“记录转 Excel 行”的转换函数，避免预览和导出不一致。
- 图片提取完成后，5 项草稿保存在 SQLite 的 `extracted_draft_json` 中，防止刷新页面后丢失。
- 草稿记录不属于已完成记录，不参与正式导出。
- 前端以数据库草稿为数据源，在 `base_write_row + completed_count` 位置叠加黄色临时行；这行只是预览覆盖层，不会写入模板文件。
- 用户确认后重新请求预览接口，临时行变成后端返回的正式行。
- 正式记录统一按 `id` 升序排列，预览和导出行号都使用 `base_write_row + zero_based_index` 计算。
- 如果模板、字段映射、开始写入行或历史记录发生变化，前端必须刷新预览。

---

## 13. 示例图片验收数据

对于示例图片：

```text
手写中名：二点红蝽
地点标签：深圳龙岗园山景区
日期标签：X-24, 2009
采集人：空白
单位：深圳职业技术学院
编号标签：PSZP-00842
```

预期结果：

```json
{
  "中名": "二点红蝽",
  "Phylum": "Arthropoda",
  "纲": "昆虫纲",
  "Class": "Insecta",
  "Order": "Hemiptera",
  "中文科名": "红蝽科",
  "科名": "Pyrrhocoridae",
  "属名": "Dysdercus",
  "种名": "cingulatus",
  "产地3": "龙岗园山景区",
  "图像": "PSZP-00842",
  "采集人": "",
  "采集日期": "2009-10-24"
}
```

---

## 14. 前端视觉要求

- 中文界面。
- 浅色主题。
- 主色使用墨绿色或蓝绿色，符合自然标本主题。
- 背景使用浅灰白。
- 卡片圆角适中，阴影轻微。
- 表单密度适中，避免大面积空白。
- 状态使用清晰的颜色标签。
- 上传区域必须醒目。
- 图片和字段必须能在一个桌面屏幕内并排查看。
- 图片与识别表单位于上方，Excel 实时预览占据下方全宽区域。
- Excel 预览区域高度默认约为浏览器可视高度的 40%，允许用户拖动调整高度。
- Excel 预览必须具有电子表格视觉：列字母、行号、网格线、固定表头和滚动条。
- 表格表头固定。
- 所有异步操作有加载状态。
- 操作成功或失败使用 Toast 提示。
- 删除操作需要二次确认。

---

## 15. 错误处理

需要处理：

- 未配置模型时点击识别
- 未上传 Excel 模板时点击导出
- 模型 API 连接失败
- Base URL 不是 API 根地址
- 模型不支持图片输入或文本 JSON 分类
- 模型超时
- 模型返回空内容
- 模型返回非法 JSON
- 中名或图像编号为空，无法确认入表
- 图像编号与已有记录重复
- 分类字段校验失败
- 图片格式不支持
- 图片 EXIF 纠正、旋转或压缩预处理失败
- Excel 模板损坏
- 目标工作表不存在
- 目标表头不存在
- Excel 实时预览加载失败
- Excel 预览与当前字段映射不一致
- Excel 正在被其他程序占用
- 数据库记录保存失败
- 导出目录不可写

错误信息必须使用中文，并告诉用户下一步应该怎么做。

---

## 16. 开发阶段清单

## 阶段一：项目骨架

- [ ] 创建 React + Vite + TypeScript 前端
- [ ] 创建 FastAPI 后端
- [ ] 配置 Tailwind CSS
- [ ] 配置路由和侧边栏
- [ ] 配置 SQLite 和 SQLAlchemy
- [ ] 创建数据目录
- [ ] 完成前后端连通测试

## 阶段二：设置页面

- [ ] 完成模型 API 配置表单
- [ ] 明确 `base_url` 是 API 根地址
- [ ] 完成包含最小图片调用和最小文本 JSON 调用的测试连接
- [ ] 完成提示词编辑
- [ ] 完成 Excel 模板上传
- [ ] 完成工作表选择
- [ ] 完成表头检测
- [ ] 完成字段列映射
- [ ] 完成开始行和格式来源行设置
- [ ] 计算并保存唯一的 `base_write_row`

## 阶段三：AI 识别

- [ ] 完成单张图片上传
- [ ] 完成图片预览
- [ ] 完成旋转和缩放
- [ ] 完成 EXIF 纠正、实际旋转、RGB 转换和长边压缩
- [ ] 确保前端旋转角度应用于真实模型输入
- [ ] 完成只依赖 `base_url`、`api_key`、`model_name` 的通用视觉 API 调用
- [ ] 完成 Base64 图片传输
- [ ] 完成 JSON 解析和重试
- [ ] 使用同步 HTTP 返回结果，不实现 `job_id` 轮询或后台任务
- [ ] 第一次模型调用只返回 5 个图片原始信息字段
- [ ] 完成 5 个图片信息字段的可编辑确认表单
- [ ] 中名和图像编号为空时禁止确认入表
- [ ] 产地3或采集日期为空时警告但允许继续，采集人允许为空
- [ ] 完成图像编号重复检查、覆盖或取消流程
- [ ] 完成“重新识别”功能
- [ ] 完成“确认信息并自动入表”功能
- [ ] 未确认图片信息时锁定分类字段
- [ ] 完成置信度和警告显示
- [ ] 成功后显示 Excel 行号，保持 2 秒高亮再自动进入下一张

## 阶段四：分类缓存

- [ ] 创建 taxonomy_cache 表
- [ ] 分类只使用 `confirmed_extraction_json` 中的中名
- [ ] 相同中名优先读取缓存
- [ ] 缓存缺失时使用模型分类
- [ ] 图片信息确认后自动生成 8 个分类字段
- [ ] 对 8 个分类字段执行完整性和格式校验
- [ ] 首次分类校验失败后自动纠正重试 1 次
- [ ] 自动合并成最终 13 个字段并保存
- [ ] 只有分类校验通过后才更新缓存
- [ ] 用户修改分类且校验通过后才同步缓存
- [ ] 分类失败时保留已确认的 5 项信息并允许单独重试分类

## 阶段五：记录管理

- [ ] 完成记录列表
- [ ] 完成搜索和筛选
- [ ] 完成编辑
- [ ] 完成删除
- [ ] 完成查看原图
- [ ] 完成重新识别
- [ ] 完成图片信息确认、分类整理和已完成状态
- [ ] 分别显示图片识别失败和分类失败状态

## 阶段六：Excel 导出

- [ ] 加载原始模板
- [ ] 复制模板生成导出文件
- [ ] 按 `id` 升序读取已完成记录
- [ ] 使用 `base_write_row + zero_based_index` 计算写入行
- [ ] 根据映射写入 13 个字段
- [ ] 复制格式来源行样式
- [ ] 正确写入日期
- [ ] 保证其他列不变
- [ ] 生成下载文件
- [ ] 完成导出汇总页面

## 阶段七：Excel 实时预览

- [ ] 创建 `preview_service.py`
- [ ] 创建 Excel 预览接口
- [ ] 完成目标工作表表头和模板数据读取
- [ ] 完成“全部列/13 个目标字段”切换
- [ ] 完成列字母和行号显示
- [ ] 完成固定表头和滚动
- [ ] 完成缩放控制
- [ ] 完成待确认黄色临时行
- [ ] 从 SQLite 草稿生成临时行，刷新页面后仍可恢复
- [ ] 完成表单修改与临时行实时同步
- [ ] 完成分类并正式入表后的绿色高亮
- [ ] 完成自动滚动到最新行
- [ ] 完成下一条写入行提示
- [ ] 完成预览状态栏
- [ ] 完成虚拟滚动或分段加载
- [ ] 保证预览顺序与导出顺序一致
- [ ] 明确预览是 Excel 风格数据网格，不承诺像素级还原复杂 Excel 样式

## 阶段八：界面完善

- [ ] 完成加载状态
- [ ] 完成 Toast 提示
- [ ] 完成空状态
- [ ] 完成错误状态
- [ ] 完成删除确认弹窗
- [ ] 完成桌面端自适应
- [ ] 统一颜色、间距和字体

## 阶段九：交付

- [ ] 提供完整源代码
- [ ] 提供 requirements.txt
- [ ] 提供 package.json
- [ ] 提供 `.env.example`
- [ ] 提供数据库初始化逻辑
- [ ] 提供 Windows `start.bat`
- [ ] 提供 Linux/macOS `start.sh`
- [ ] 提供中文 README
- [ ] 提供模板配置说明
- [ ] 提供模型 API 配置说明

---

## 17. 测试清单

- [ ] 首次启动自动创建数据库和数据目录
- [ ] 能上传提供的 Excel 模板
- [ ] 能正确识别目标工作表
- [ ] 能正确匹配 13 个字段
- [ ] 模板配置时能正确计算并保存 `base_write_row`
- [ ] 模板上传后能立即显示目标工作表预览
- [ ] 预览区能切换全部列和 13 个目标字段
- [ ] 能上传示例图片
- [ ] Base URL 填写 API 根地址时能正常拼接请求路径
- [ ] 测试连接能分别验证图片输入和文本 JSON 分类
- [ ] 不支持图片输入或文本分类的模型会被明确拦截
- [ ] 能正常同步调用模型 API，不依赖 `job_id` 轮询
- [ ] EXIF 方向能正确纠正
- [ ] 用户旋转后的方向会用于模型输入，不只是前端显示
- [ ] 超大图片能按规则压缩，原图保持不变
- [ ] 能解析合法 JSON
- [ ] 非法 JSON 能自动重试
- [ ] 采集人不存在时保持空白
- [ ] 罗马数字月份能正确转换
- [ ] “图像”提取为标签编号而不是文件名
- [ ] 产地3能去除深圳上级地名
- [ ] 第一次识别只返回 5 个图片原始信息字段
- [ ] 图片信息未确认前不会调用分类模型
- [ ] 用户能够点击重新识别并获得新的 5 项结果
- [ ] 用户能够直接修改识别错误的 5 项信息
- [ ] 中名为空时不能确认入表
- [ ] 图像编号为空时不能确认入表
- [ ] 采集人为空时可以继续
- [ ] 产地3或采集日期为空时显示警告并可继续
- [ ] 图像编号重复时不会静默新增记录
- [ ] 重复编号可选择覆盖已有记录或取消
- [ ] 分类过程只使用用户最终确认的中名
- [ ] 确认后系统能自动生成 8 个分类字段
- [ ] 确认后不需要第二次人工确认即可完成入表
- [ ] 分类字段不完整或格式错误时会自动纠正重试
- [ ] 分类校验失败时保留 5 项确认信息并标记“分类失败”
- [ ] 分类失败记录能够单独重试分类，不重新识别图片
- [ ] 无效分类结果不会写入缓存
- [ ] 相同中名只会使用通过校验的分类缓存
- [ ] 用户能修改 5 个图片原始信息字段
- [ ] 修改表单时黄色临时预览行实时同步
- [ ] 刷新页面后能从 SQLite 恢复待确认草稿
- [ ] 保存后记录出现在记录管理页
- [ ] 分类完成后临时行补齐 8 个分类字段并转为正式行
- [ ] 正式入表后最新行绿色高亮
- [ ] 成功提示包含实际 Excel 行号，并在约 2 秒后进入下一张
- [ ] 预览区能自动滚动到最新写入行
- [ ] 预览区能显示下一条写入行
- [ ] 删除记录后不进入导出结果
- [ ] 编辑或删除记录后预览能够正确刷新
- [ ] 只导出已完成记录
- [ ] 预览和导出都按记录 `id` 升序排列
- [ ] 每条记录行号都等于 `base_write_row + zero_based_index`
- [ ] 删除记录后预览和导出按相同规则重新紧凑排列
- [ ] Excel 预览数据与最终导出数据一致
- [ ] Excel 只修改 13 个目标字段
- [ ] 原模板不被覆盖
- [ ] 导出文件能被 Microsoft Excel 正常打开
- [ ] 导出日期显示为 `YYYY-MM-DD`
- [ ] 图像编号不被 Excel 自动转换

---

## 18. 验收标准

项目验收必须同时满足：

1. 用户能够在设置页面上传模板并保存字段映射。
2. 用户只需填写 `base_url`、`api_key`、`model_name` 即可配置或切换同时支持图片输入和文本分类的 OpenAI 兼容模型。
3. 用户能够单张上传图片并首先获得 5 个图片原始信息字段。
4. 用户能够重新识别，也能够直接修改识别错误的 5 项信息。
5. 缺失采集人不会被自动编造。
6. 日期能够从常见标签格式转换为标准日期。
7. 未确认图片信息前，系统不会生成分类信息。
8. 中名和图像编号为空时不能确认入表，其他允许缺失的字段按规则警告或留空。
9. 图像编号重复时不会静默新增，用户可以覆盖已有记录或取消。
10. 用户确认后，系统只使用确认后的中名补全 8 个分类字段。
11. 8 个分类字段只有通过自动校验后才能完成入表和写入缓存。
12. 分类失败时保留 5 项确认信息并可单独重试。
13. 分类整理、记录保存和 Excel 填表不需要第二次人工确认。
14. 相同中名能够复用已经通过校验的分类信息。
15. 用户能够管理、编辑和删除识别记录。
16. 识别工作台能够通过只读 Excel 风格数据网格实时显示目标工作表数据和进度。
17. 图片信息在确认前能够以黄色临时行显示，分类字段保持为空；页面刷新后草稿仍可恢复。
18. 用户修改图片信息时，Excel 临时行能够同步更新。
19. 分类完成后，预览区补齐 8 个分类字段、显示实际写入行号并自动定位最新行。
20. Excel 预览和导出使用同一 `base_write_row`、同一记录顺序及同一行号公式。
21. 用户能够导出新的 Excel 文件。
22. 导出只写入指定字段，不破坏 `base_write_row` 前的原有数据、其他单元格和样式。
23. 原始模板始终保留。
24. 项目能按照 README 在 Windows 电脑上运行。

---

## 19. 给开发模型的执行要求

请严格按本文实现一个完整、可运行的项目，而不是只提供伪代码或页面演示。

执行要求：

- 先检查开发环境。
- 创建完整前后端目录和代码。
- 逐阶段实现并运行测试。
- 不要省略数据库、Excel 导出或模型调用。
- 不要使用假数据代替真实接口。
- 模型接口只能要求用户填写 Base URL、API Key 和模型名称，不得增加服务商选择、超时、重试次数或其他必填配置。
- Base URL 必须按 API 根地址实现，同一模型必须通过最小图片输入和最小文本 JSON 分类测试。
- 图片旋转必须影响真正的模型输入；必须保留原图，并按本文完成 EXIF 纠正和尺寸预处理。
- 图片提取、重新识别和确认分类统一使用同步 HTTP 响应，不得同时实现 `job_id` 轮询或后台任务版本。
- 必须严格执行“提取 5 项图片信息 → 用户重新识别或修改 → 用户确认 → 自动分类 → 自动入表”的顺序。
- 未经用户确认的中名不得用于分类，分类接口只能读取 `confirmed_extraction_json`。
- “中名”和“图像”为空时不得确认入表；图像编号重复时必须让用户覆盖或取消，不得静默产生重复记录。
- 用户确认 5 项图片信息后，不得再增加第二个人工确认步骤。
- 8 个分类字段必须自动校验；校验失败只能进入“分类失败”并保留 5 项确认信息，不得写入缓存或正式 Excel 记录。
- 必须实现可视化 Excel 实时预览，不能用普通记录列表代替。
- Excel 预览必须支持待确认临时行、表单同步、确认后刷新和自动定位最新行。
- 待确认草稿必须保存到 SQLite；黄色临时行是前端覆盖层，不得把草稿写进正式导出。
- 模板配置时必须保存 `base_write_row`；预览和导出都按记录 `id` 升序及 `base_write_row + zero_based_index` 计算行号。
- 成功后必须显示“已写入 Excel 第 N 行”，高亮约 2 秒后再自动进入下一张。
- 前端必须是中文并具备完整交互状态。
- 完成后实际启动项目，修复启动错误。
- 使用示例图片和 Excel 模板执行一次端到端测试。
- 最终提供运行命令、默认访问地址和配置说明。

---

## 20. 技术参考

- FastAPI 文件上传：<https://fastapi.tiangolo.com/tutorial/request-files/>
- openpyxl 读取工作簿：<https://openpyxl.readthedocs.io/en/3.1/tutorial.html>
- openpyxl 单元格样式：<https://openpyxl.readthedocs.io/en/3.1/styles.html>
- 智谱视觉模型文档：<https://docs.bigmodel.cn/cn/guide/models/vlm/glm-4.6v>
- 阿里云千问视觉理解：<https://help.aliyun.com/zh/model-studio/vision-model/>
- 火山方舟图片理解：<https://www.volcengine.com/docs/82379/1362931>
